<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Button background color -->
    <stroke android:width="2dp" android:color="@android:color/black"/> <!-- Border line -->
    <corners android:radius="4dp"/> <!-- Rounded corners if desired -->
    <padding android:left="10dp" android:top="10dp"
        android:right="10dp" android:bottom="10dp"/> <!-- Padding inside the button -->
</shape>
